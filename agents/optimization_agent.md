# Optimization Agent — AI Insights & Recommendations

## Overview

Compares performance metrics across persona segments and generates a structured optimization report: a natural language summary, next topic recommendations, and CTA improvements.

## Agents

### OptimizationAgent
Uses `client.messages.parse()` with a Pydantic schema to produce a validated `OptimizationReport` object.

## Status

> 🚧 To be designed — see [README.md](./README.md) for pipeline context.
